{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run COBOL",
            "type": "cobol",
            "request": "launch",
            "program": "${file}",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal"
        }
    ]
}