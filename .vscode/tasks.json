{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compile and Run COBOL",
            "type": "shell",
            "command": "cobc -x ${file} -o ${fileDirname}/${fileBasenameNoExtension} && ${fileDirname}/${fileBasenameNoExtension}",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "presentation": {
                "reveal": "always",
                "panel": "shared"
            },
            "problemMatcher": ["$gcc"]
        }
    ]
}